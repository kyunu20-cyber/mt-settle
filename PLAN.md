# MT 정산 웹 — 기획/구현 계획서

> 상태: **계획(승인 대기)** · 작성일 2026-08-03 · 독립 범용 정산 웹

---

## 1. 한 줄 정의

관리자가 물품 가격이나 쇼핑 링크를 넣으면 **자동으로 합계·1인당 금액을 계산**하고,
**회비 납부/미납 현황**을 정리해서, 참가자는 링크로 **결과만 조회**하는 웹.

## 2. 확정된 결정 (이번 대화에서 정한 것)

| 항목 | 결정 |
|---|---|
| 링크 처리 | 자동채움 경로 2개 — **① 직접 입력(최우선) ② 쿠팡 "공유" 텍스트 붙여넣기 파싱**. 실시간 스크래핑은 쿠팡 상품페이지가 서버 접근을 403으로 막아 **제외**. 링크 자체는 근거로 저장 |
| 정산 방식 | 1차는 **단순 계산**(총합·1인당). 회비는 **누가 냈나/안 냈나** 정리 |
| 입력/공개 | **관리자 입력 → 참가자 읽기전용 조회** (호스팅 웹 + 공유 저장소 필요) |
| 프로젝트 | **독립 범용 정산 웹** (이번 MT 외 재사용) |

## 3. 핵심 기능 (2개 모듈)

### A. 지출 정산
- 항목 추가: **이름 / 금액 / (선택) 결제자 / (선택) 링크**
- 금액 입력 경로 2가지
  - **직접 입력** (최우선)
  - **공유텍스트 붙여넣기**: 쿠팡 앱 "공유" 시 복사되는 `상품명 + 가격 + 링크` 텍스트를 붙여넣으면 상품명·금액·링크를 파싱해 자동 채움 **(제안값)**
- **자동 채우기 규칙**: 파싱값은 회색 제안으로만 표시. 사용자가 금액을 손대면 그 값이 최종(제안값과 달라도 수동이 이김). 파싱 실패해도 링크는 저장하고 금액만 수동.
- 자동 계산: **총 지출**, **참가 인원 ÷ = 1인당 금액**
- (Phase 2) 결제자 기록 → "누가 누구에게 얼마" 송금 정리

### B. 회비 관리
- 멤버 명단 등록
- 멤버별 **납부 여부 체크 + 금액**
- 요약: **걷힌 총액 / 미납자 명단 / 목표 대비**

## 4. 화면 구성

| 화면 | 접근 | 내용 |
|---|---|---|
| 관리자 편집 | 편집용 링크(비밀 토큰) | 항목·회비 입력/수정, 참가 인원 설정, 공유 링크 복사 |
| 참가자 조회 | 공개 링크(읽기전용) | 지출 합계·1인당 금액, 회비 납부 현황. 편집 불가 |

- 한 "정산판(board)" = 한 이벤트. 편집 링크 1개 + 조회 링크 1개.
- 로그인/회원가입 없음. **비밀 토큰이 든 편집 링크**를 아는 사람만 수정.

## 5. 기술 스택 & 아키텍처

- **프론트엔드**: React + Vite + TypeScript (기존 앱들과 동일 스택)
- **저장/공유**: Supabase (Postgres + 자동 REST) — 무료 티어, 상태공유 해결
- **공유텍스트 파싱**: 서버리스 불필요 — 붙여넣은 텍스트를 **클라이언트에서 정규식 파싱**(상품명·가격·URL 추출)
- **호스팅**: Vercel(프론트) + Supabase(데이터). "나중에 공개" = Vercel 배포 URL

```text
[관리자 브라우저] --편집--> Supabase(정산판 데이터) <--조회-- [참가자 브라우저]
        |
        +--공유텍스트 붙여넣기--> (클라이언트 파싱) --> 상품명·가격 제안
```

## 6. 데이터 모델 (초안)

```
board        id · title · admin_token · member_names[] · created_at
expense      id · board_id · name · amount · payer · link · amount_source(manual/paste)
due(회비)    id · board_id · member · amount · paid(bool) · paid_at
```

- 조회 링크는 board_id만, 편집 링크는 board_id + admin_token.
- 쓰기는 admin_token 검증 후 허용, 읽기는 공개.

## 7. 링크 자동채움 — 상세

- **검증 결과(2026-08-03)**: 쿠팡 상품페이지(`coupang.com/vp/products/...`)를 서버에서 fetch하면 **HTTP 403 Access Denied**. 실시간 스크래핑은 헤드리스+프록시가 필요해 개인 정산엔 과함 → **채택 안 함**.
- **채택 방식 = 공유텍스트 파싱**: 쿠팡 앱 "공유"로 복사되는 텍스트(`[쿠팡] 상품명 … 가격원 링크` 형태)를 붙여넣으면 **클라이언트에서 정규식으로** 상품명·가격·URL 추출.
- **원칙**: 파싱값은 제안일 뿐, **직접 입력이 최종 권위**. 파싱 실패해도 링크는 저장.
- **확인 필요**: 쿠팡 공유텍스트에 실제로 가격 숫자가 포함되는지 실기기 1회 확인(포함 안 되면 상품명·링크만 자동, 금액은 수동).

## 8. 구현 단계

- **Phase 1 (MVP)**: 프로젝트 셋업 → 관리자 편집/참가자 조회 화면 → 지출 항목(수동 금액) 합계·1인당 → 회비 납부 체크 → Supabase 저장 → Vercel 배포
- **Phase 2**: 공유텍스트 붙여넣기 파싱(클라이언트) + 자동/수동 우선 규칙 UI
- **Phase 3**: 결제자별 송금 정리, 조회 화면 다듬기, (선택) 영수증 캡처 OCR

> Phase 1만으로 "관리자 입력 → 참가자 조회 + 회비 정리"는 완결됨. 링크 자동화는 Phase 2로 분리해 리스크 격리.

## 9. 리스크 / 확인 필요

- **링크 자동화**: 실시간 스크래핑은 쿠팡 403으로 검증 완료 → 제외. 공유텍스트 파싱으로 대체(남은 확인 = 공유텍스트 가격 포함 여부, 7절)
- **공개 시 개인정보**: 참가자 **실명 그대로 노출로 확정**(닉네임 마스킹 없음). 조회 링크를 아는 사람은 실명+금액을 봄 → MT 내부 공유 전제로 진행
- **편집 링크 유출**: 토큰만 알면 수정 가능 → MT 규모(수십 명)엔 충분, 더 엄격히 하려면 비번 추가

## 10. 다음 액션

1. 이 계획 승인 여부 (수정 요청 환영)
2. 실제 붙일 쇼핑 링크 샘플 2~3개 (자동추출 성공률 검증용)
3. 승인 시 Phase 1부터 구현 착수
